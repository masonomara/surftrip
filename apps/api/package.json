{
  "name": "@docket/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predeploy": "npm run kb:manifest",
    "deploy": "npm run predeploy && wrangler d1 migrations apply docket-db --remote && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "test:e2e": "vitest --config vitest.e2e.config.mts",
    "test:all": "CLOUDFLARE_ACCOUNT_ID=$(grep CLOUDFLARE_ACCOUNT_ID .dev.vars | cut -d= -f2) vitest run && vitest run --config vitest.e2e.config.mts",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "kb:manifest": "npx tsx scripts/generate-kb-manifest.ts"
  },
  "dependencies": {
    "@docket/shared": "*",
    "better-auth": "^1.4.7",
    "drizzle-orm": "^0.41.0",
    "resend": "^6.6.0",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@types/node": "^25.0.3",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.56.0"
  }
}
